<Window x:Class="TrafficViolation.NotificationControll.NotificationsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficViolation.NotificationControll"
        mc:Ignorable="d"
        Title="NotificationsView" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dgNotification" AutoGenerateColumns="False" HeadersVisibility="Column"
      ItemsSource="{Binding Notification}" Margin="38,29,106,82"
      IsReadOnly="False">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Date" Binding="{Binding SentDate,Mode=TwoWay}" Width="100" IsReadOnly="True"/>

                <!-- Nút Edit -->
                <DataGridTemplateColumn Header="Edit" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit" Padding="5,2"
                        Click="ViewButton_Click"
                        CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
            </DataGrid>


    </Grid>
</Window>
